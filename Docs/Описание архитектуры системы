Составитель: Халемский Никита Дмитриевич БПИ201
Общая информация о компьютерной системе:
    ОС: Ubuntu 20.04.3 LTS
    Процессор: AMD Ryzen 3 2200G

Информация о программе:
    Используемые типы данных:
        int - 4 байта
        double - 8 байт
        bool - 1 байт

    Общее число заголовочных файлов (.h файлов) - 6
    Общее число модулей реализации (.cpp файлов) - 7

    Описание структур:
    1) container (Всего 400008 байт)
        enum {max_len = 10000} - 4 байта[0]
        int len - 4 байтах[4]
        shape cont[max_len] - 400000 байт[8]

    2) shape (Всего 40 байт)
        enum key {RECTANGLE, TRIANGLE, CIRCLE} - 4 байта[0]

        enum color {RED, ORANGE, YELLOW, GREEN, BLUE, 
        INDIGO, VIOLET} - 4 байта[4]

        key shape_key - 4 байта[8]

        color shape_color - 4 байта[12]

        union { - максимум 24 байта[16]
            rectangle r - 17 байт
            triangle t - 24 байта
            circle c - 16 байт
        };

    3) rectangle (Всего 17 байт)
        int highLeftX - 4 байта[0]
        int highLeftY - 4 байта[4]
        int lowRightX - 4 байта[8]
        int lowRightY - 4 байта[12]
        bool isValidate - 1 байт[16]

    4) triangle (Всего 24 байт)
        int first_x - 4 байта[0]
        int first_y - 4 байта[4]
        int second_x - 4 байта[8]
        int second_y - 4 байта[12]
        int third_x - 4 байта[16]
        int third_y - 4 байта[20]

    5) circle (Всего 16 байт)
        int center_x - 4 байта[0]
        int center_y - 4 байта[4]
        double radius - 8 байт[8]


